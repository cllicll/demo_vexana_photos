import 'package:flutter/material.dart';
import './photos_view.dart';

class Photos extends StatefulWidget {
  const Photos({Key? key}) : super(key: key);

  @override
  PhotosView createState() => PhotosView();
}
